pipeline {
  agent any
  tools {
    maven 'maven-3.9.2'
  }
  stages {
      stage('Build') {
       steps {
       echo 'manoj'
       }
      }
      stage('test') {
       steps {
       bat 'mvn clean'
       }
      }
    
  }   
}
