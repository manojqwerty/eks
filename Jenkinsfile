pipeline {
  agent any
  tools {
    maven 'mvn 3.5.2'
  }
  stages {
      stage('Build') {
       steps {
       echo 'manoj'
       }
      }
      stage('Build') {
       steps {
       bat 'mvn clean'
       }
      }
    
  }   
}
